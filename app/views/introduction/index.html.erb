<% flash_messages %>
<% @users.each do |user| %>
<%= user.username %>
<%= form_for current_user.notifications_of_from_user.build(opponent_user_id: user.id), :url => {:controller => :introduction, :action => :post} do |f| %>

  <%= f.hidden_field :opponent_user_id %>
  <%= submit_tag '通知を送る' %>
<% end %>
<% end %>
